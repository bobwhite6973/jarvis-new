# JARVIS - Clean requirements
# Removed: eel, pyautogui, pyaudio, vosk, pyttsx3, edge-tts, pvporcupine,
#           opencv, pywin32, psycopg, chromadb, g4f, nara, webscout (desktop/Windows deps)

# Core async
httpx==0.27.0
python-dotenv==1.0.1

# Telegram
python-telegram-bot==21.5

# LLM SDKs
anthropic==0.34.0
groq==0.10.0
openai==1.40.0

# Utilities
colorama==0.4.6
